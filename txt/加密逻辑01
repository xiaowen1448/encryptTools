一、功能目标

本程序用于对用户拖入的 文件或文件夹进行加密或解密操作。

设计目标：

支持 拖拽文件 / 文件夹

支持 递归处理文件夹

支持 两种加密模式

防止文件二次加密

解密时能够 准确识别合法加密文件

未勾选覆盖源文件时，所有输出统一写入 output 目录

output 目录 始终保持一致，不出现多层嵌套

二、文件加密识别机制

程序必须通过 文件头标识（Magic Header） 判断文件是否已经加密。

不能只依赖文件扩展名。

原因：

用户可能修改扩展名

文件可能被误处理

仅靠扩展名无法保证准确识别

因此所有加密文件必须写入固定的 文件头结构。

三、加密文件结构设计

每一个加密文件在文件开头写入 Header 结构。

Header 长度固定为 16 字节。

结构如下：

偏移	长度	内容
0	8	Magic 标识
8	1	版本号
9	1	加密类型
10	6	预留

Magic 字符串：

WXENC001

示例：

57 58 45 4E 43 30 30 31

含义：

WXENC001

字段说明：

Magic
用于识别文件是否为本程序加密文件

Version
加密格式版本号

EncryptType
表示使用的加密类型

1 = 加密类型一
2 = 加密类型二

Reserved
预留字段，用于未来扩展

Header 后面紧接 加密后的文件数据

四、加密文件扩展名

程序支持两种加密类型，对应不同扩展名。

加密类型	扩展名
类型1	.enc1
类型2	.enc2

示例：

原文件

test.txt

加密后

test.txt.enc1

或

test.txt.enc2
五、文件加密前检测逻辑（防止二次加密）

当用户点击 执行加密 时，程序需要先判断文件是否已经加密。

检测步骤：

打开文件

读取前 16 字节

判断 Magic 是否为

WXENC001

判断规则：

如果 Magic 匹配

文件已经加密
跳过该文件

如果 Magic 不匹配

执行加密流程

该机制可以防止：

二次加密

用户手动修改扩展名导致的误判

六、解密文件识别逻辑

当用户点击 执行解密 时，程序必须判断文件是否为合法加密文件。

步骤：

读取文件前 16 字节

判断 Magic 是否为

WXENC001

处理逻辑：

如果 Magic 不匹配

提示：不是有效加密文件
跳过该文件

如果 Magic 匹配

读取 EncryptType
调用对应解密算法

注意：

解密判断 必须依赖文件头，而不是文件扩展名

七、拖入文件和文件夹处理逻辑

程序支持拖入：

单个文件

多个文件

文件夹

如果拖入的是 文件夹

程序需要 递归扫描文件夹

扫描规则：

遍历目录
    如果是文件
        加入处理列表
    如果是目录
        递归扫描
八、加密模式文件过滤

在加密模式下：

程序需要跳过已经加密的文件。

判断方法：

读取文件头 Magic。

如果 Magic 为：

WXENC001

则跳过该文件。

九、解密模式文件过滤

在解密模式下：

程序只处理加密文件。

判断方法：

读取文件头 Magic。

如果 Magic 不等于：

WXENC001

则跳过。

十、输出目录逻辑

如果用户 未勾选“覆盖源文件”

所有处理结果写入统一目录：

output

output 目录位置：

拖入路径的同级目录

示例：

拖入目录

D:\data\files

输出目录

D:\data\output
十一、output 目录创建规则

程序必须确保：

output 目录只创建一次

不能出现：

output/output
output/output/output

因此需要检测：

如果拖入路径本身已经位于 output 目录中，则向上寻找。

示例：

拖入路径

D:\data\output\files

程序应识别：

真实 output 目录

为

D:\data\output

而不是再创建

output/output
十二、输出文件命名规则

加密输出：

原文件名 + 加密扩展名

示例：

test.txt
→
test.txt.enc1

解密输出：

恢复原始文件名

示例：

test.txt.enc1
→
test.txt
十三、覆盖源文件模式

如果用户勾选 覆盖源文件

处理规则如下。

加密：

test.txt
→ 加密 → test.txt.enc1
→ 删除原文件 test.txt

解密：

test.txt.enc1
→ 解密 → test.txt
→ 删除原文件 test.txt.enc1
十四、错误处理规则

程序需要处理以下异常情况：

1 文件无法读取
跳过并记录日志

2 文件头损坏
提示文件损坏

3 解密类型未知
跳过文件

4 输出目录创建失败
提示错误

十五、完整流程
加密流程
拖入文件或目录
        ↓
递归扫描文件
        ↓
逐个读取文件头
        ↓
如果已加密
        ↓
跳过
        ↓
否则执行加密
        ↓
写入 Header
        ↓
输出到 output 目录
解密流程
拖入文件或目录
        ↓
递归扫描文件
        ↓
读取文件头
        ↓
判断 Magic
        ↓
不是加密文件
        ↓
跳过
        ↓
读取 EncryptType
        ↓
调用对应解密算法
        ↓
输出原文件